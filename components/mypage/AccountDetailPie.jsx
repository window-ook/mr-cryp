import { useEffect, useState } from 'react';

export default function AccountDetailPie({ balance }) {
  return (
    <div className="w-full p-8 lg:py-1 lg:px-8 border-4 border-main rounded-t-[0.25rem] shadow-lg bg-white flex justify-center"></div>
  );
}
